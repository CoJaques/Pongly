package pongly.common;

/**
 * This enum is used to represent messages sent between the server and the client
 */
public enum Message {
    UPDATE_PLAYER,
    UPDATE_SERVER,
    UPDATE_SCORE,
    QUIT,
    READY
}
